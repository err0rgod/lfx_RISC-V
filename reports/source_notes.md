# Source Notes

The exact challenge snippets are stored in `inputs/snippets.yaml`.

Supplied reference URL:

```text
https://docs.riscv.org/reference/isa/v20260120/priv/priv-csrs.html
```

The original challenge text embedded the same URL twice. That URL points to CSR-related documentation, while the supplied cache snippet is labeled `Privileged Spec 19.3.1`. This repository records the mismatch neutrally and does not use the URL to add facts to the extraction results.

Check date: 2026-07-26.

External material was excluded from the extraction prompt and from the final human-reviewed result. The result only contains facts supported by exact substrings of the supplied snippets.
